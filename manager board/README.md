The manager board implementation is served from `manager-board/`.

Vercel's serverless file bundler can behave poorly with spaces in function asset paths, so the deployable files live in the hyphenated folder while this folder remains as the marker requested for the manager board work.
